> [!IMPORTANT]
> Avoid rewriting published git history — force pushing, rebasing, amending, or squashing commits that are already pushed — as it can rewrite history on the connected editor side and the user may lose project history.
>
> Keep the connected branch in a working state so changes sync cleanly.
